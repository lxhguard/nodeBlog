global:
-process对象
    >process对象是一个全局对象，可以在任何地方都能访问到他，通过这个对象
        提供的属性和方法，使我们可以对当前运行的程序的进程进行访问和控制
    >argv
        -Array,一个包含命令行参数的数组
    >execPath
        -开启当前进程的绝对路径
    >env
        -返回用户环境信息
    >Buffer类:Buffer类是一个全局变量类型，用来直接处理2进制数据。它能够用多种方式构建。